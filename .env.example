AUTO_DEV_API_KEY=
IMAGE_PROXY_SECRET=
NODE_ENV=development
LOG_ENABLED=false
PUBLIC_BASE_URL=https://findmycar.getcarwise.app

# Live Edmunds VIN-listing confirmation via Google Custom Search API
# (lib/edmunds-search.ts). Both required together — feature stays inert
# (fails open to pre-existing behavior, zero regression) until both are set.
# See lib/edmunds-search.ts module doc for how to obtain these and for
# cost/volume expectations before enabling in production.
# CONCRETE BLOCKER as of 2026-09-03: not yet provisioned.
GOOGLE_CSE_API_KEY=
GOOGLE_CSE_CX=
